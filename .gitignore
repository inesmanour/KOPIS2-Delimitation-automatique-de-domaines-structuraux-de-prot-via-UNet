############################
# Python / projet
############################

__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# Packages / build
*.egg-info/
dist/
build/

############################
# Environnements
############################

.venv/
venv/
env/
.conda/
*.env

############################
# Jupyter
############################

.ipynb_checkpoints/
.jupyter/

############################
# OS / éditeurs
############################

.DS_Store
Thumbs.db
.vscode/
.idea/

############################
# Résultats / modèles
############################

# On ne cache plus results/ pour le montrer complet sur GitHub
# (si besoin tu pourras ignorer certains sous-dossiers plus tard)

# Fichiers de modèles / checkpoints
*.pt
*.pth
*.ckpt

# Logs éventuels
logs/
*.log
*.out
*.err

############################
# Données KOPIS (gros fichiers)
############################

# ASTRAL : on montre tout (pas d'ignore spécifique)
# (si tu veux masquer certains gros fichiers, on pourra affiner plus tard)
# kopis_data/astral/*

# SIFTS : on montre tout
# (on enlève l'ignore des .tsv/.tsv.gz)
# kopis_data/sifts/*.tsv
# kopis_data/sifts/*.tsv.gz

# AlphaFold : ignorer tout sauf UN fichier d'exemple
kopis_data/alphafold/*
!kopis_data/alphafold/AF-A0A0A0RDR2.cif

# Sorties SWORD2 : ignorer tout sauf UN dossier d'exemple
kopis_data/sword/*
!kopis_data/sword/A0A0A0RDR2/

# Données ML : on montre tout le dossier ml, y compris les .npy
# (on garde l'ignore global des .npy mais on le réactive pour ml/)
*.npy
!kopis_data/ml/**
# et on n'ignore pas le dossier ml lui-même
# (aucune ligne du type "kopis_data/ml/" ici)

############################
# Divers
############################

# Si un jour tu fais un dossier data/ en plus
data/